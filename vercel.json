{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && if [ -n \"$DATABASE_URL\" ] && [ \"${DATABASE_URL#file:}\" = \"$DATABASE_URL\" ]; then prisma db push --skip-generate; fi && next build",
  "installCommand": "npm ci"
}
